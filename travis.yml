language: ruby
rvm:
 - 2.3.3
 dist: trusty
 sudo: false

branches:
  only:
  - integrating-travis-ci

before_install:
  - gem install bundler

install:
  - bundle install
  - bundle exec rails db:setup
